module.exports = {
    testMatch: ['**/tests/**/*.test.js'], // specify the test file pattern
    testEnvironment: 'node', // set the test environment
    reporters: ['default'], // specify the reporter(s) to use
  };
  